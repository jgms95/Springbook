<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="b.k.s">

   <select id="getIno" resultType="Integer">
      <![CDATA[select max(ino) from item]]>
   </select>

   <insert id="insert">
      <![CDATA[insert into item (ino,ititle,iwriter,publishDay,publisher,cataCode,filename,content,price,percent,stock,discountedPrice)
      values (#{ino}, #{ititle}, #{iwriter}, #{publishDay}, #{publisher}, #{cataCode}, #{filename}, #{content}, #{price},#{percent},#{stock},#{discountedPrice})]]>
   </insert>
   
   
   <delete id="delete">
      delete from item where ino = #{ino}
   </delete>
   
   

   <select id="list" resultType="ItemDTO">
      <![CDATA[select * from item order by ino desc]]>
   </select>
   
   	<select id="getAmount" resultType="Integer">
		select count(ino) from item
	</select>
	
	  	<select id="searchgetAmount" resultType="Integer">
		select count(ino) from item where cataCode = #{cataCode}
	</select>
	
	
	<select id="updateui" resultType="ItemDTO">
		select * from item where ino = #{ino}
	</select>
	
	<update id="update">
			update item set  ititle = #{ititle}, iwriter = #{iwriter}, publishDay= #{publishDay}, publisher = #{publisher}, cataCode= #{cataCode}, filename= #{filename}, content= #{content}, price = #{price}, percent =#{percent}, writeday = sysdate, stock = #{stock}, discountedPrice = #{discountedPrice}       
		where ino = #{ino}
	</update>
	
	<select id="searchlist" resultType="ItemDTO">
			select * from item where cataCode = #{cataCode}  order by ino desc
	</select>
	
	
</mapper>